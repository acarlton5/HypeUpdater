# HypeUpdater

Tiny update signal repo for HypeShell.

- `fingerprint` contains the latest HypeShell build fingerprint.
- `latest.json` contains metadata for the Settings > About update panel.

When HypeShell ships a new installer build, update this fingerprint to match `BUILD_FINGERPRINT` in `HYPESHELL-Installer/install.sh`.
